# 2026-09-21

- 在 `NewCode/输入输出练习/` 新建 `00_输入输出模板总结.md`，总结本目录 13 个 I/O 练习文件的输入方式、T 组/EOF 多组、含空格字符串、输出格式、SPJ 浮点、万能模板与速查表。目的：备战 ACM 刷题。经两轮精简+补充，最终为纯代码片段速查（读入/输出/拼装三块），含 readline/buffer/字符矩阵/图边/十六进制/交互题 flush/print(*arr)/大整数等。
- 在 `测试/` 新建 `00_ACM导包速查.md`：因用户部分题为 LeetCode 形式忽略了导包，整理 ACM(牛客/Codeforces) 需自备的内容——①标准库模块及用法(sys/math/collections/heapq/itertools/bisect/functools/re/string/random/typing)；②LeetCode 隐式提供的 ListNode/TreeNode 标准定义及数组建树建链表的辅助函数（用户 剑指 Offer 的 TreeNode 默认 val=-1 与标准版不同）；③第三方库(sortedcontainers/numpy/torch)多数 OJ 没有需避免。并指出类型标注 List/Optional 在 ACM 可整行删掉。
